export const internalConfigFile = `import {defineConfig} from '@etools/generator'
export default defineConfig({
    // 变量集
    variables: {
        
    },
    // 功能模块
    modules: {
        routes: {
            hooks: [],
            variables: {

            }
        }
    }
})
`;
